import { Role } from '@entity/role'
import { db } from 'src/app'

const leaveTypeSeeds = async () => {
  // eslint-disable-next-line no-console
  console.info( 'Seeding Role data' )
}

export default leaveTypeSeeds
